{
  "name": "pg-explain-viewer",
  "version": "0.7.2",
  "description": "PostgreSQL query plan parser and visualizer (text/JSON/YAML), zero runtime dependencies",
  "main": "src/pgplan.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "python3 build.py"
  },
  "license": "MIT",
  "devDependencies": {
    "terser": "5.51.2"
  }
}
